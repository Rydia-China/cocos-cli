# Lunaverse Build Runner

Internal build runner used by Lunaverse CI packaging jobs.

This package is not a public SDK or a general-purpose development tool. The exposed `cocos` binary is kept only for compatibility with the existing Moonshort build scripts.

## CI Install

```bash
npm install -g mobai-lv-build-runner@0.0.1-alpha.29
cocos --version
```
